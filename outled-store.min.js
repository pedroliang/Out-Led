!function(){"use strict";const o="outled_admin_db_url";function r(){try{return localStorage.getItem(o)||""}catch(o){return""}}let t=null;function n(){return t||(t=import("https://esm.sh/@neondatabase/serverless@0.9.5")),t}async function e(o){return(await n()).neon(o)}function a(o){return{id:o.id,name:o.name||"",codigo:o.codigo||"",cat:o.cat||"",catLabel:o.cat_label||"",oldPrice:Number(o.old_price)||0,price:Number(o.price)||0,img:o.img||Array.isArray(o.photos)&&o.photos[0]||null,photos:Array.isArray(o.photos)?o.photos:[],videos:Array.isArray(o.videos)?o.videos:[],desc:o.description||"",condition:o.condition||"",icon:o.icon||"projector",color:o.color||""}}function i(o){return{id:o.id,label:o.label||"",icon:o.icon||"projector"}}function c(o){return new Promise((r,t)=>{const n=new FileReader;n.onload=()=>r(n.result),n.onerror=t,n.readAsDataURL(o)})}async function s(){const o=r();if(!o)throw new Error("Credencial admin não configurada. Entre em Modo Edição e cole a connection string.");return e(o)}window.OutLedStore={init:async function(){try{await n()}catch(o){}},isRemote:function(){return!0},hasAdminCredentials:function(){return!(!(o=r())||!String(o).trim());var o},setAdminCredentials:async function(r){if(!r||!/^postgres(ql)?:\/\//i.test(r))throw new Error("Connection string inválida. Cole a string que começa com postgresql:// fornecida pelo Neon.");const t=await e(r);try{const o=await(t`
        SELECT has_table_privilege(current_user, 'products', 'INSERT') AS can_write
      `);if(!o[0]||!o[0].can_write)throw new Error("Esta credencial não tem permissão de escrita. Use a connection string da role 'neondb_owner' (admin).")}catch(o){throw new Error("Falha ao validar credencial: "+(o.message||o))}return function(r){try{localStorage.setItem(o,r||"")}catch(o){}}(r),!0},clearAdminCredentials:function(){!function(){try{localStorage.removeItem(o)}catch(o){}}()},loadAll:async function(){try{const o=await async function(){return e("postgresql://public_ro:DphLXPru9KybTuYicnpj@ep-flat-moon-ac4ya8nv-pooler.sa-east-1.aws.neon.tech/neondb?sslmode=require")}(),r=await(o`SELECT * FROM products ORDER BY created_at ASC`),t=await(o`SELECT * FROM categories ORDER BY sort_order ASC`);return{products:r.map(a),categories:t.map(i)}}catch(o){throw console.error("[OutLedStore.loadAll] falha:",o),new Error("Não consegui ler do banco Neon: "+(o.message||o))}},saveProduct:async function(o){const r=await s();return await(r`
      INSERT INTO products (
        id, name, codigo, cat, cat_label, old_price, price,
        img, photos, videos, description, condition, icon, color, updated_at
      ) VALUES (
        ${String(o.id)}::varchar,
        ${o.name||""}::varchar,
        ${o.codigo||null}::varchar,
        ${o.cat||null}::varchar,
        ${o.catLabel||null}::varchar,
        ${Number(o.oldPrice)||0}::numeric,
        ${Number(o.price)||0}::numeric,
        ${o.img||null}::text,
        ${JSON.stringify(o.photos||[])}::jsonb,
        ${JSON.stringify(o.videos||[])}::jsonb,
        ${o.desc||null}::text,
        ${o.condition||null}::varchar,
        ${o.icon||null}::varchar,
        ${o.color||null}::varchar,
        NOW()
      )
      ON CONFLICT (id) DO UPDATE SET
        name = EXCLUDED.name,
        codigo = EXCLUDED.codigo,
        cat = EXCLUDED.cat,
        cat_label = EXCLUDED.cat_label,
        old_price = EXCLUDED.old_price,
        price = EXCLUDED.price,
        img = EXCLUDED.img,
        photos = EXCLUDED.photos,
        videos = EXCLUDED.videos,
        description = EXCLUDED.description,
        condition = EXCLUDED.condition,
        icon = EXCLUDED.icon,
        color = EXCLUDED.color,
        updated_at = NOW()
    `),o},deleteProduct:async function(o){const r=await s();await(r`DELETE FROM products WHERE id = ${String(o)}::varchar`)},saveCategory:async function(o,r){const t=await s();return await(t`
      INSERT INTO categories (id, label, icon, sort_order, updated_at)
      VALUES (
        ${String(o.id)}::varchar,
        ${o.label||""}::varchar,
        ${o.icon||null}::varchar,
        ${Number(r)||0}::int,
        NOW()
      )
      ON CONFLICT (id) DO UPDATE SET
        label = EXCLUDED.label,
        icon = EXCLUDED.icon,
        sort_order = EXCLUDED.sort_order,
        updated_at = NOW()
    `),o},deleteCategory:async function(o){const r=await s();await(r`DELETE FROM categories WHERE id = ${String(o)}::varchar`)},uploadPhoto:async function(o){return c(o)},uploadVideo:async function(o){return c(o)}}}();
